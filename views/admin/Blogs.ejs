<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Blogs</title>
</head>
<body>
    <h2>Admin Blogs</h2>
    <a href="/admin/blogs/add-blog">Add Blog</a>
    <table>
        <thead>
            <tr>
                <th>Title</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
            <!-- Loop through blogs and display each one -->
            {{#each blogs}}
            <tr>
                <td>{{this.Blogtitle}}</td>
                <td>
                    <a href="/admin/blogs/edit-Blog/{{this.slug}}">Edit</a>
                    <a href="/admin/blogs/delete-Blog/{{this._id}}" onclick="return confirm('Are you sure you want to delete this blog?')">Delete</a>
                </td>
            </tr>
            {{/each}}
        </tbody>
    </table>
</body>
</html>
